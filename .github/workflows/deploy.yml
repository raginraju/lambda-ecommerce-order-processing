name: Deploy Poultry Shop

on:
  push:
    branches: [ main, test ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Links to GitHub Environment rules (Manual Approval for 'prod')
    environment: ${{ github.ref == 'refs/heads/main' && 'prod' || 'test' }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Dependencies
        run: |
          npm install -g aws-cdk
          pip install -r requirements.txt
          cd frontend && npm install && cd ..

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-southeast-1

      - name: Set Environment Stage
        run: |
          if [[ ${{ github.ref }} == 'refs/heads/main' ]]; then
            echo "STAGE=prod" >> $GITHUB_ENV
          else
            echo "STAGE=test" >> $GITHUB_ENV
          fi

      # --- LAYERED DEPLOYMENT FLOW ---

      # Step 1: Deploy Infrastructure ONLY
      # This won't crash because it doesn't look for the 'dist' folder yet
      - name: Deploy Infrastructure
        run: |
          cdk deploy "${{ env.STAGE }}-PoultryInfraStack" \
            -c stage=${{ env.STAGE }} \
            --require-approval never \
            --outputs-file outputs.json

      # Step 2: Build Frontend
      # Now we have the real API_URL from the InfraStack outputs
      - name: Build Frontend
        run: |
          # Note: The stack name in jq must match exactly what is in app.py
          API_URL=$(jq -r ".\"${{ env.STAGE }}-PoultryInfraStack\".ApiUrl" outputs.json)
          USER_POOL=$(jq -r ".\"${{ env.STAGE }}-PoultryInfraStack\".UserPoolId" outputs.json)
          CLIENT_ID=$(jq -r ".\"${{ env.STAGE }}-PoultryInfraStack\".UserPoolClientId" outputs.json)
          
          echo "VITE_API_URL=$API_URL" > frontend/.env
          echo "VITE_COGNITO_USER_POOL_ID=$USER_POOL" >> frontend/.env
          echo "VITE_COGNITO_CLIENT_ID=$CLIENT_ID" >> frontend/.env
          
          cd frontend
          npm run build
          cd ..

      # Step 3: Deploy Frontend Assets
      # This triggers the BucketDeployment now that the 'dist' folder exists
      - name: Deploy Frontend Assets
        run: |
          cdk deploy "${{ env.STAGE }}-PoultryDeployStack" \
            -c stage=${{ env.STAGE }} \
            --require-approval never